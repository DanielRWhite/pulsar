// use crate::window::Window;
// use std::sync::{ Arc, Mutex };
// use winit::dpi::PhysicalSize;

// pub struct Settings<'a> {
//         window: &'a Window
// }

// impl<'a> Settings<'a> {
//         pub fn new(window: &'a Window) -> Self {
//                 Settings { window }
//         }

//         pub fn get_window_sizes(&self) -> Vec<PhysicalSize<u32>> {
//                 todo!()
//         }
// }